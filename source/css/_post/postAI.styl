.post-ai
  background var(--rin-secondbg)
  border-radius 12px
  padding 12px
  line-height 1.3
  border var(--style-border-always)
  margin-top 16px
  min-height 101.22px
  box-shadow var(--rin-shadow-border)

  +maxWidth768()
    margin-top 22px

  .ai-title
    display flex
    color var(--rin-lighttext)
    border-radius 8px
    align-items center
    user-select none

    .ai-title-left
      display flex
      align-items center
      color var(--rin-lighttext)

      i.ai-title-icon
        width 24px
        height 24px
        display flex
        background var(--rin-lighttext)
        color var(--rin-card-bg)
        font-size 14px
        border-radius 20px
        justify-content center
        align-items center

      .ai-title-text
        font-weight 700
        margin-left 8px
        line-height 1
        font-size 14px

    .ai-tag
      font-size 12px
      background-color var(--rin-lighttext)
      box-shadow var(--rin-shadow-main)
      color var(--rin-card-bg)
      font-weight 700
      border-radius 12px
      margin-left auto
      line-height 12px
      padding 6px 8px
      display flex
      align-items center
      justify-content center
      cursor pointer
      transition .3s

      &.loadingAI
        animation-duration 2s
        animation-name AILoading
        animation-iteration-count infinite
        animation-direction alternate
        cursor default

      &:hover:not(.loadingAI)
        background var(--rin-fontcolor)
        color var(--rin-card-bg)

  .ai-explanation
    margin-top 12px
    padding 8px 12px
    background var(--rin-card-bg)
    border-radius 8px
    border var(--style-border-always)
    font-size 15px
    line-height 1.4
    display none
    text-align left

    .blinking-cursor
      background-color var(--rin-lighttext)
      width 14px
      height 14px
      border-radius 16px
      display inline-block
      vertical-align middle
      animation blinking-cursor 2s infinite
      margin-left 4px
      margin-bottom 3px
      transform scale(.6)

  .ai-suggestions
    display flex
    flex-wrap wrap

    .ai-suggestions-item
      margin-top 12px
      padding 8px 12px
      background var(--rin-card-bg)
      border-radius 8px 8px 8px 0
      border var(--style-border-always)
      font-size 14px
      line-height 1.4
      display flex
      width fit-content
      margin-right 12px
      cursor pointer
      transition .3s

      &:hover
        background var(--rin-main)
        color var(--rin-white)

  .ai-bottom
    width 100%
    margin-top 12px
    display flex

    .ai-tips
      font-size 12px
      color var(--rin-secondtext)
      margin-right auto

    .ai-report
      font-size 12px
      color var(--rin-secondtext) !important
      white-space nowrap
      border-bottom none!important

      &:hover
        background: none !important