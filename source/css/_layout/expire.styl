if hexo-config("expire.enable")
  .expire
    display flex
    align-items center
    border-radius 8px
    background-color rgba(255,0,0,.5)
    border-left 5px solid var(--rin-red)
    padding 10px 16px
    color var(--rin-fontcolor)

    i.solitude
      color var(--rin-fontcolor)
      margin-right 8px